import { useState } from "react";

/*
Requirements:
- Create inputs for:
  - Name
  - Email
- Store values using state (controlled inputs) / onChange + value={some state variable}
- On submit:
  - Prevent page reload
  - Display submitted values or conditional success message
- Clear form after submission
*/

export default function ContactForm() {
  return <div></div>;
}
