import react, { useState } from "react";


function taskObject({name, completed,deleted} : Task) {

}

function newTask(name:string) {
  
}

export const Practice = () => { 

    const gameSetExample = {
        id: 1,
        Game_Name: "World Of Warcraft"
    }

    const [game, setGame] = useState("");
    const [numberOfGame,setGameNumber] =  useState();
    const [saved,setSaved]= useState(false);
    const [gameType, setGameType] = useState();
    const [gameSets,setGameSets] = useState();




    return (

        <div>
            <h2>{game}</h2>
            <input className="border-1" onChange={(e) => setGame(e.target.value)} value={game}  />
            <button><button/>

            
            <input className="border-1" onchange={(e) => setGameNumber(e.target.value)} value={numberOfGame}>


        </div>

    )
}