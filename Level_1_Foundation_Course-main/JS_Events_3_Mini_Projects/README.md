# Understanding JavaScript Events — 3 Mini Projects

You are learning **JavaScript Events** — the way JavaScript can “listen” for user actions and respond.

Think of it like this:

- The **browser** is a room.
- The **user** does things (clicks, types, hovers).
- JavaScript can **listen** for those actions using `addEventListener()`.
- When the action happens, JavaScript runs a **function**.

---

## What you will practice in this bundle

### ✅ Event types covered

- `click` (button click)
- `mouseover` and `mouseout` (hover in / hover out)
- `keydown` and `keyup` (keyboard typing events)
- `change` (input/select value changed)
- `removeEventListener()` (turning an event off)

### ✅ Core skills you will repeat

- Selecting DOM elements with `document.querySelector()`
- Writing event listeners with `element.addEventListener("eventName", function(){ ... })`
- Using the **event object**: `event`
- Updating the UI with `textContent`
- Adding/removing CSS classes with `classList.add()` / `classList.remove()`

---

## Projects inside this bundle

### Project 01 — **Click Counter + Toggle Panel**

**Focus:** `click` events  
You will build:

- A counter that increases each click
- A reset button
- A toggle button that shows/hides a panel

### Project 02 — **Hover Mood Card + Keyboard Mirror**

**Focus:** `mouseover`, `mouseout`, `keydown`, `keyup`  
You will build:

- A card that changes style on hover in/out
- A “typing mirror” that shows what you type
- A little live “key info” display using the event object

### Project 03 — **Select Theme + Remove Event Listener**

**Focus:** `change` + `removeEventListener()`  
You will build:

- A theme dropdown that changes the page theme
- A “Start Listening / Stop Listening” demo button
- When stopped, clicks do nothing (listener removed)

---

## Run instructions

1. Open any project folder.
2. Open **index.html** in your browser.
3. Open `script.js` and follow the steps.
4. Refresh the browser after you save changes.

Have fun — events are where pages start feeling “alive”. 🎛️✨
